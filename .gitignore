# ── 依赖与构建产物 ──────────────────────────────────────────────
node_modules/
release/
dist/

# ── 图标（由 `npm run icon` 生成，不入库） ───────────────────────
assets/icon.png
assets/icon-256.png
assets/tray.png
assets/tray@2x.png

# ── 日志与临时文件 ──────────────────────────────────────────────
*.log
dist-log.txt
preview-status.html
.tmp-*
.shot-app/
.npm-cache/
.electron-cache/
.dsh-test/
.user-data-test/
.ws-test/

# ── 上游迭代备份（本仓库改用 git 历史，不再保留 .bak） ──────────
*.bak
*.bak-*
*.orig
*.rej

# ── 本地临时产物（许可清单生成的中间文件、提交信息草稿等） ──────
_gen-*
_commitmsg.txt
_tmpmsg.txt

# ── 编辑器 / 系统 ───────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini
