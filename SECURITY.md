# 安全策略 / Security Policy

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞。**

请通过以下任一私下渠道联系维护者：

- GitHub 私密漏洞报告：<https://github.com/18477514055/DeepseekHarness-Desktop/security/advisories/new>
- 或在该仓库开 issue 时仅写"需要私下沟通安全事项"，不透露细节，由维护者建立私密渠道。

请在报告中包含：

- 受影响的版本（`package.json` 的 `version`）
- 复现步骤或 PoC
- 影响评估（能做什么、需要什么前置条件）
- 若涉及第三方依赖，注明是哪一个包与版本

我们会在确认后尽快回应，并在修复发布后致谢（除非你希望匿名）。

## 本项目的安全边界（请先理解，再评估影响）

本应用是一个 **Electron 桌面外壳**，它把官方 dsh 服务器跑在
**本机回环地址 `127.0.0.1`** 上，然后加载官方前端。

### 已经做对的默认设置

| 项 | 值 | 位置 |
|---|---|---|
| `contextIsolation` | `true` | `src/main.js` 的 `createWindow()` |
| `nodeIntegration` | `false` | 同上 |
| `sandbox` | `true` | 同上 |
| 外链处理 | 一律交给系统浏览器，应用内不跳转 | `setWindowOpenHandler` |
| 导航拦截 | 仅允许 `serverUrl` 与 `data:`（状态页） | `will-navigate` |

### 已知的、**属于设计取舍**而非漏洞的点

以下几点是**刻意的设计选择**，报告前请先确认它们是否真的构成问题：

1. **服务器监听在回环地址，无额外认证。**
   这是上游 dsh 的模型：任何本机进程都能访问该端口。桌面层复用的正是这个模型
   （"你开着网页版时自动复用同一个服务器"）。把它改成需要认证会直接破坏
   与官方网页版/CLI 的会话互通。**若你认为这需要改，请作为设计讨论提出，而非漏洞报告。**

2. **子进程以当前用户权限运行。**
   服务器进程与桌面外壳同权限运行，这是桌面应用的常规模型。

3. **`preload.js` 暴露的通道。**
   它刻意保持最小。若你发现某条通道能被不受信任的页面内容利用
   （例如经 `serverUrl` 内容注入），**那才是真正需要报告的问题**。

4. **未签名的可执行文件。**
   本应用没有代码签名证书，杀软误报属常见现象（见 README 常见问题）。
   "未签名"本身不是漏洞。

### 真正值得报告的问题类型

- 通过 `serverUrl` 加载的内容能突破 `contextIsolation` / 触达 Node 能力
- `will-navigate` / `setWindowOpenHandler` 的绕过（如能实现任意代码执行或凭据外泄）
- 日志文件（`shell.log` / `server.log`）写入敏感信息
- 构建/发布流程被投毒（例如 `patch-dsh.js` 的补丁被劫持）
- 第三方依赖中的已知高危漏洞（我们会评估是否需要升级）

## 依赖与许可风险

本项目打包 590 个第三方包，其中包含 **1 个 LGPL-3.0-or-later 组件**
（`@img/sharp-win32-x64`，以独立 DLL 动态加载）。
许可层面的说明见 [`THIRD-PARTY-LICENSES.md`](THIRD-PARTY-LICENSES.md)。

依赖清单可用 `npm run licenses` 重新生成并核对。

## 支持范围

只支持**最新版本**。本项目基于官方 **rc（预发布）** 版本，
不保证与官方后续版本兼容；升级官方内核后请重新评估本节内容。
