# 合规交接文档（给"负责打包"的对话）

> 本文件是**交接说明**，不是分发包的一部分。
> 目的：让打包对话据此把 `resources/app` 这个分发包**改到合规**，然后再出包。
> **本文件本身不应随分发包发布**——打包时请把它移出 `files` 列表或删除。
> 写于 2026-09-18，基于本机已安装版本 `0.1.2-rc.1`（路径 `resources/app`）。

---

## 〇、一句话结论

这个桌面端不是从零写的，是**社区开发者 `chyra-moon` 的封装 + 你的重度魔改**。
要把它作为"可分发的桌面端"发布，**必须保留 chyra-moon 的 MIT 署名（法律强制，不是客气）**，
并补上**第三方依赖许可清单**与**钉死的官方版本号**。

归因链（三层，发布时全要点名）：

```
deepseek-ai（官方核心 @deepseek-ai/dsh，MIT）
   └─ chyra-moon（社区 Electron 桌面封装，仓库 github.com/chyra-moon/deepseek-harness-desktop，MIT）
        └─ 你（在本机重度魔改；跨端协作插件 dsh-crosshub 是你独立新增的部分）
```

---

## 一、已核实的事实（证据，file:line 可复核）

1. **外壳原作者 = chyra-moon**
   - `README.md:7-11`：徽章与发布全部指向 `chyra-moon/deepseek-harness-desktop`；
     原文："⚠️ 社区项目，非官方出品。DeepSeek Harness 本体、`@deepseek-ai/*` 软件包与官方前端版权归 deepseek-ai 及其贡献者所有。"
   - 但 `package.json:7` 的 `author` 写的是泛化的 `"DeepSeek Harness Desktop"`，
     没有点名 chyra-moon —— **这是合规缺口之一**（见第三节）。

2. **官方核心层全是 MIT**
   - `node_modules/@deepseek-ai/dsh/package.json`、`dsh-web`、`dsh-app-boot`、`dsh-client-ui-renderer`
     等 229 个官方包：`license = MIT`，`repository = git+https://github.com/deepseek-ai/deepseek-harness.git`，
     版本统一 `0.1.2-rc.1`。

3. **第三方依赖 200+ 个，各有自己的 LICENSE**
   - `node_modules/` 下 **200+ 个** npm 包（electron-updater、openai、react、koffi、express、sharp…）
     每个都自带 LICENSE 文件（MIT/ISC/BSD/Apache 等）。
   - 当前分发包**只有你自己的那一份 MIT**（`resources/app/LICENSE`），把别人的声明全吞掉了 —— **合规缺口之二**。

4. **当前分发包里没有 NOTICE / THIRD-PARTY-LICENSES / 第三方清单文件**
   - 只在 `node_modules/*/LICENSE` 里物理存在，顶层没有汇总。

5. **你确实做了大量改动（魔改证据）**
   - `src/main.js` 有 **6 个 `.bak-时间戳` 备份**（最旧 702 行 → 当前 843 行，迭代明显）。
   - `main.js` 引用官方包时**只 `require` 其 bin 入口，从不 monkey-patch / 改 `prototype`**
     （`grep` 结果：对 `@deepseek-ai/dsh` 全为加载型引用，无 `.prototype.`/patch）。
   - 版本钉死逻辑 `BUNDLED_DSH_SPEC`（`main.js:85-92`）：故意把核心钉到同一版本，
     注释写明"不同世代会激活失败"——这是你的封装层设计，不是改内核。
   - 你新增的最大改动 **dsh-crosshub（跨端协作）已经独立成包**（见工作区 `04-其他项目/dsh-crosshub`），
     这部分可以干净地署你自己的名。

---

## 二、归因与权属（改分发包前先认清楚）

- **deepseek-ai**：官方核心著作权，MIT。必须保留其版权/许可声明。
- **chyra-moon**：桌面外壳原始著作权，MIT。**必须保留其版权行与许可声明**（MIT 强制条款：
  "The above copyright notice … shall be included in all copies or substantial portions"）。
  改得再面目全非，它仍是他的作品的一份实质部分，删署名 = 违反 MIT。
- **你**：拥有"修改权" + 在**你的修改部分**上的署名权；外壳的**原始著作权仍属 chyra-moon**。
  你真正 100% 可干净署名的增量是 **dsh-crosshub 跨端插件**（本就独立成包）。

---

## 三、分发包当前缺口（打包对话要补的）

| # | 缺口 | 现状 | 要求 |
|---|---|---|---|
| 1 | chyra-moon 署名 | `package.json:7` author 含糊；README 虽提了仓库但 LICENSE 没点名 | LICENSE 版权行须原样保留/补写 chyra-moon 名字与仓库 |
| 2 | 第三方依赖许可清单 | 只有你自己的 MIT，200+ 包声明被吞 | 新增 `THIRD-PARTY-LICENSES`（或 `NOTICE`），汇总所有第三方包许可 |
| 3 | 官方版本号钉死声明 | 只说"基于官方"，没钉具体版本 | 声明"基于 github.com/deepseek-ai/deepseek-harness @ 0.1.2-rc.1" |
| 4 | rc 版本风险提示 | 无 | 注明"基于未正式发布的 rc 版本，不保证与官方后续版本兼容" |
| 5 | 你的改动说明 | 无 | README/ABOUT 列出你做的优化与 dsh-crosshub 插件 |
| 6 | 跨端插件归属 | 混在一起 | dsh-crosshub 作为独立文件/独立仓库，单独署名 |

---

## 四、打包对话应执行的动作清单

**保留（不许动/不许删）：**
- `node_modules/*/LICENSE`（200+ 个第三方许可文件）—— 物理保留即满足 MIT/ISC 的保留义务；
- `README.md` 里 chyra-moon 的仓库与"社区项目非官方出品"声明；
- 现有 `LICENSE` 的 MIT 正文。

**新增：**
1. **改 `LICENSE` 顶部版权行**：在原 MIT 之上/之下明确写
   `Copyright (c) 2026 chyra-moon (github.com/chyra-moon/deepseek-harness-desktop)`，
   并在其后追加你的修改署名行。MIT 正文保持原样。
2. **新增 `THIRD-PARTY-LICENSES`**：
   用 `license-checker --summary` 或类似工具，从 `node_modules` 生成全部第三方包及其许可证清单，
   随包发布（可放在 `resources/app` 顶层，并确保打进 `build.files`）。
3. **改 `README.md` 的"许可"段**（现 `README.md:61-63`）：
   写明三层归因（deepseek-ai → chyra-moon → 你 + 跨端插件），
   并注明钉死的官方版本号 `0.1.2-rc.1` 与 rc 风险提示。
4. **新增"本修改版改动说明"小节**：列出你的优化
   （版本钉死防起不来、attach 复用已有官方服务器、断线自愈、GPU 硬加速 fail-fast 规避、
   托盘/单实例/窗口记忆/崩溃自启、FreeConsole 等）与 dsh-crosshub 跨端协作插件。
5. **dsh-crosshub 单独成文件**：确保它作为独立插件包存在、单独署名，
   不混进官方核心层（目前工作区已是独立仓库，打包时维持这种分离）。

**校验：**
- 出包后用 `license-checker` 复核第三方清单完整；
- 确认 `package.json` 的 `build.files` 把 `THIRD-PARTY-LICENSES` 与改过的 `LICENSE` 包含进去；
- 确认 chyra-moon 署名与 deepseek-ai 官方仓库链接都在发布物里可见。

---

## 五、给打包对话的"第一句话"（可直接复制）

> 接手 DeepSeek Harness 桌面端合规改造：先读
> `C:\Users\24239\AppData\Local\Programs\DeepSeek Harness\resources\app\00-合规交接-给打包对话.md`，
> 然后按第四节清单补 chyra-moon 署名、第三方许可清单、钉版本号与改动说明，使分发包满足 MIT 保留义务。

---

*本交接文档由只读核查生成，未修改分发包内任何现有文件。*
